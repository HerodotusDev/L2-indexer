apiVersion: apps/v1
kind: Deployment
metadata:
  name: l2-indexer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: l2-indexer
  template:
    metadata:
      labels:
        app: l2-indexer
    spec:
      imagePullSecrets:
        - name: dockerhub-secret           
      containers:
        - name: l2-indexer
          image: <IMAGE>
          envFrom:
            - secretRef:
                name: l2-indexer-secret
          resources:
            requests:
              cpu: '2'
              memory: '10G'
            limits:
              cpu: '2'
              memory: '16G'
          livenessProbe:
            httpGet:
              path: /is-alive
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /is-alive
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
---    
---
apiVersion: v1
kind: Service
metadata:
  name: l2-indexer-service
spec:
  selector:
    app: l2-indexer
  ports:
    - port: 8080
      targetPort: 8080