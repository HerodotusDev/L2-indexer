apiVersion: apps/v1
kind: Deployment
metadata:
  name: l2-indexer-worker-base-sepolia
spec:
  replicas: 1
  selector:
    matchLabels:
      app: l2-indexer-worker-base-sepolia
  template:
    metadata:
      labels:
        app: l2-indexer-worker-base-sepolia
    spec:
      imagePullSecrets:
        - name: dockerhub-secret           
      containers:
        - name: l2-indexer-worker-base-sepolia
          image: <IMAGE>
          envFrom:
            - secretRef:
                name: l2-indexer-worker-base-sepolia-secret
          resources:
            requests:
              cpu: '1'
              memory: '128Mi'
            limits:
              cpu: '1'
              memory: '128Mi'
---    
---
apiVersion: v1
kind: Service
metadata:
  name: l2-indexer-worker-base-sepolia-service
spec:
  selector:
    app: l2-indexer-worker-base-sepolia
  ports:
    - port: 8080
      targetPort: 8080